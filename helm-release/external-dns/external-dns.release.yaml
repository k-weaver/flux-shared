apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: external-dns
  namespace: kube-system
spec:
  interval: 5m
  dependsOn:
    - name: external-dns-repo
      namespace: kube-system
  chart:
    spec:
      chart: external-dns
      version: ''
      sourceRef:
        kind: HelmRepository
        name: external-dns-repo
  # valuesFrom:
  #   - configMapKeyRef:
  #       name: external-dns-common-values
  #       namespace: kube-system
  #       key: common.values.yaml
